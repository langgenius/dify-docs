diff --git a/node_modules/@mintlify/mcp/bin/utils/cli-config.js b/node_modules/@mintlify/mcp/bin/utils/cli-config.js
index 0635f15..629ff45 100644
--- a/node_modules/@mintlify/mcp/bin/utils/cli-config.js
+++ b/node_modules/@mintlify/mcp/bin/utils/cli-config.js
@@ -66,8 +66,9 @@ function initializeToolsFromConfig(tools, mcpDir) {
         console.log(`🛠  ${colors.bold}Installing Tools:${colors.default}`);
         console.log();
         for (const tool of tools) {
-            console.log(`   ${tool.tool.name}${colors.default}`);
-            console.log(`   ${colors.dim}${tool.tool.description}${colors.default}`);
+            const toolObj = tool.tool || tool;
+            console.log(`   ${toolObj.name}${colors.default}`);
+            console.log(`   ${colors.dim}${toolObj.description}${colors.default}`);
             console.log();
         }
         fs.writeFileSync(path.join(mcpDir, 'src', 'tools.json'), JSON.stringify(tools, null, 2));
