---
title: "30分钟快速入门"
icon: "forward"
---

<Note> ⚠️ 本文档由 AI 自动翻译。如有任何不准确之处，请参考[英文原版](/en/documentation/pages/getting-started/quick-start)。</Note>


Dify 的真正价值在于无论多么复杂的想法，你都能轻松地构建、部署和扩展。它专为快速原型设计、流畅迭代和任何级别的可靠部署而构建。

让我们从学习可靠的大型语言模型集成到你的应用程序开始。在本指南中，你将构建一个简单的聊天机器人，它可以对用户的问题进行分类，使用大型语言模型直接回答，并用特定国家的趣味事实增强回答。

<iframe 
  className="w-full aspect-video rounded-xl" 
  src="https://www.youtube.com/embed/dJ34OU_JY7Y?si=AiYJq7WY1iUCgGAO"
  title="Dify Quick Start Video" 
  frameBorder="0" 
  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
  allowFullScreen 
/>

## 步骤 1：创建新工作流 (2 分钟)

1. 前往 **Studio** > **Workflow** > **Create from Blank** > **Orchestrate** > **New Chatflow** > **Create**

## 步骤 2：添加工作流节点 (6 分钟)

<Tip>
  当你想要引用任何变量时，首先输入 `{` 或 `/`，然后就可以看到工作流中可用的不同变量。
</Tip>

### 1. LLM 节点和输出：理解并回答问题

<Info>
  `LLM` 节点将提示词发送给语言模型，根据用户输入生成响应。它抽象了 API 调用、速率限制和基础设施的复杂性，让你可以专注于设计逻辑。
</Info>

<Steps>
  <Step title="创建 LLM 节点">
    使用 `Add Node` 按钮创建一个 LLM 节点，并将其连接到你的开始节点
  </Step>
  
  <Step title="配置模型">
    选择一个默认模型
  </Step>
  
  <Step title="设置系统提示词">
    将以下内容粘贴到系统提示词字段中：

    ```text
    The user will ask a question about a country. The question is {{sys.query}}  
    Tasks: 
    1. Identify the country mentioned. 
    2. Rephrase the question clearly. 
    3. Answer the question using general knowledge. 
    
    Respond in the following JSON format: 
    {   
      "country": "&lt;country name&gt;",
      "question": "&lt;rephrased question&gt;",
      "answer": "&lt;direct answer to the question&gt;" 
    }
    ```
  </Step>
  
  <Step title="启用结构化输出">
    **启用结构化输出** 让你能够轻松控制大型语言模型的返回内容，并确保一致的、机器可读的输出，用于下游的精确数据提取或条件逻辑。
    
    - 将输出变量结构化切换为开启 > `Configure` 并点击 `Import from JSON`
    - 粘贴：
    
    ```json
    {   
      "country": "string",   
      "question": "string",   
      "answer": "string" 
    }
    ```
  </Step>
</Steps>

### 2. 代码块：获取趣味事实

<Info>
  `Code` 节点使用代码执行自定义逻辑。它让你能够在需要的地方精确注入代码——在可视化工作流中——为你节省连接整个后端的工作。
</Info>

<Steps>
  <Step title="创建代码节点">
    使用 `Add Node` 按钮创建一个 `Code` 节点，并连接到 LLM 块
  </Step>
  
  <Step title="配置输入变量">
    将一个 `Input Variable` 名称更改为 "country"，并将变量设置为 `structured_output` > `country`
  </Step>
  
  <Step title="添加 Python 代码">
    将以下代码粘贴到 `PYTHON3` 中：

    ```python
    def main(country: str) -> dict:     
      country_name = country.lower()     
      fun_facts = {
        "japan": "Japan has more than 5 million vending machines.",
        "france": "France is the most visited country in the world.",
        "italy": "Italy has more UNESCO World Heritage sites than any other country."     
      }     
      fun_fact = fun_facts.get(country_name, f"No fun fact available for {country.title()}.")
      return {"fun_fact": fun_fact}命名输出变量">
    将输出变量 `result` 更改为 `fun_fact`，以获得标签更好的变量
  </Step>
</Steps>

### 3. 答案节点：给用户的最终答案

<Info>
  `Answer` 节点创建一个干净的最终输出以供返回。
</Info>

<Steps>
  <Step title="创建答案节点">
    使用 `Add Node` 按钮创建一个 `Answer` 节点
  </Step>
  
  <Step title="配置答案字段">
    粘贴到答案字段中：

    ```text
    Q: {{ structured_output.question }} 

    A: {{ structured_output.answer }} 

    Fun Fact: {{ fun_fact }}
    ```
  </Step>
</Steps>

结束工作流：

<img
  src="/images/quick-start-workflow-overview.png"
  alt="显示 LLM、代码和答案节点连接的完整工作流图"
  style={{ width: "100%" }}
/>

---

## 步骤 3：测试机器人 (3 分钟)

点击 `Preview`，然后询问：

- "法国的首都是什么？"
- "告诉我关于日本料理的信息"
- "描述意大利的文化"
- 任何其他问题

确保你的机器人按预期工作！

## 你已完成机器人！

本指南展示了如何可靠且可扩展地集成语言模型，而无需重新发明基础设施。通过 Dify 的可视化工作流和模块化节点，你不仅构建得更快，还采用了适用于大型语言模型驱动应用的干净、生产就绪的架构。
