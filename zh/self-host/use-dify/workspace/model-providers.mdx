---
title: "模型供应商"
description: "为工作空间添加模型供应商，让应用能够使用 LLM、Embedding 模型及其他 AI 服务"
---

> 本文档由 AI 自动翻译。如有任何不准确之处，请参考 [英文原版](/en/self-host/use-dify/workspace/model-providers)。

每个 Dify 应用都至少需要一个模型供应商。安装供应商并填入 API 密钥后，工作空间内的所有应用都能使用它的模型。

<Warning>
API 密钥授予工作空间范围的模型访问权限，并会通过各供应商的账户直接计费。只有工作空间所有者和管理员可以管理供应商。
</Warning>

## 添加供应商

1. 在 **集成** > **模型供应商** 中，浏览 **安装模型供应商** 区域，或访问 [Marketplace](https://marketplace.dify.ai/) 获取更多选项。

2. 安装完成后，点击供应商卡片上的 **设置**，填入 API 密钥（以及供应商要求的其他凭据，如自定义端点 URL 或组织 ID）。Dify 会先验证凭据，再将该供应商投入使用。

## 添加自定义模型

大多数供应商自带模型，授权其中一个后，它的模型即可使用。只有当你需要的模型不在列表中时，才需要自行添加，通常有两种情况：

- 你运行着自己的推理服务器（如 Ollama 或 Xinference），Dify 无法得知你加载了哪些模型。
- 供应商列出了自己的模型，但你需要的特定模型（如新发布或微调的模型）不在其中。

点击供应商卡片上的 **添加模型**，为模型填写名称和凭据。如果某个供应商只提供固定的一组模型，则不会显示此选项。

<Info>
若添加的自定义模型名称和类型与已有模型完全相同，Dify 会将新密钥附加到现有模型上，而不会创建重复的模型。
</Info>

## 管理凭据

当你想隔离开发与生产环境、在多个账户间分摊用量，或将不同模型指向不同服务器时，可为一个供应商添加多个密钥或端点。

密钥的管理位置取决于模型类型：
- 对于供应商自带的模型，点击供应商卡片上的 **配置**，统一管理这些模型共享的密钥。
  <Frame>
  ![管理预定义模型的凭据](/images/use-dify/workspace/predefined-model-credential.png)
  </Frame>
- 对于你添加的自定义模型，点击该模型的 **配置**，管理它自己的密钥。
  <Columns cols={2}>
    <Frame>
    ![自定义模型配置](/images/use-dify/workspace/custom-model-config.png)
    </Frame>
    <Frame>
    ![自定义模型凭据](/images/use-dify/workspace/custom-model-credential.png)
    </Frame>
  </Columns>

    <Warning>
    你添加的模型依赖于自己的凭据。删除它唯一的凭据时，该模型也会一并删除。
    </Warning>

点击供应商卡片上的 **管理凭据**，可在同一处查看所有自定义模型的密钥。

<Frame>
  ![管理所有自定义模型的凭据](/images/use-dify/workspace/custom-model-credential-list.png)
</Frame>

即使移除模型后，密钥仍保留在此处，方便你日后重新添加该模型而无需再次录入。

<Frame>
  ![重新添加已移除的模型](/images/use-dify/workspace/removed-custom-model-re-add.png)
</Frame>

## 设置默认模型

点击工具栏中的 **默认模型设置**，为每个类别指定回退模型。未单独指定模型的应用和节点会使用这些默认值：

- **系统推理模型**：默认的 LLM。
- **Embedding 模型**：用于知识库的索引和检索。
- **Rerank 模型**：按相关性对检索结果重新排序。
- **语音转文本模型**：将音频输入转换为文本。
- **文本转语音模型**：将文本回复转换为音频。
