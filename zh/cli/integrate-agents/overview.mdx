---
title: 将你的 Agent 接入 Dify 应用
sidebarTitle: 概述
description: 通过 difyctl，让任意编码 Agent 调用你的 Dify 应用，无需对接 API，也无需安装 SDK
---

> 本文档由 AI 自动翻译。如有任何不准确之处，请参考 [英文原版](/en/cli/integrate-agents/overview)。

你的 Agent 可以把 Dify 应用当作工具来调用。它将 `difyctl` 作为普通子进程运行，再读取打印回来的 JSON。`difyctl` 会自我描述，因此 Agent 能自行发现应用、运行应用并读取结果。

## Agent 自行完成的工作

Agent 会驱动一个四步循环，无需你编写任何胶水代码：

1. **发现** 工作空间中的应用。
2. **查看** 其中某个应用，了解它需要哪些输入。
3. **运行** 该应用并传入这些输入。
4. **解析** 返回的 JSON 结果。

Agent 并不依赖硬编码的命令清单。`difyctl` 通过 [`difyctl help -o json`](/zh/cli/reference/help#机器可读帮助) 自我描述，Agent 在运行时读取这些信息，因此始终与当前的 CLI 保持同步。

## 开始之前：在 Agent 运行的位置登录

Agent 不会自己登录。它运行 `difyctl`，并复用所在机器上已有的会话，因此要先在那台机器上准备好会话：

- **在你自己的机器上**：通过浏览器一次性 [登录](/zh/cli/authenticate)。
- **在服务器或容器中**：在该机器上登录，或将会话复制过去。详见 [在 Agent 运行的位置进行认证](/zh/cli/integrate-agents/auth-for-agent-deployments)。

## 接入你的 Agent

Agent 通过 `difyctl help -o json` 学习 `difyctl` 的用法。唯一要做的配置就是把它指向那里，具体方式取决于 Agent：

- **能读取技能** （Claude Code、Codex 等）：[安装 `difyctl` 技能](/zh/cli/integrate-agents/install-the-difyctl-skill)。一条命令即可写入一个小文件，把 Agent 指向 `difyctl help -o json`，它便会据此自行接入，无需任何对接代码。
- **不能读取技能**：自行为它指明方向。在 Agent 的系统提示词或指令中加上一句话，例如「要使用 Dify，请运行 `difyctl help -o json` 并使用它列出的命令」。这正是技能本应替你完成的交接。
