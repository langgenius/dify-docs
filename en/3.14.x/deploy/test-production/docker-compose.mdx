---
title: 'Docker Compose'
---

## Requirements

- Release Log: https://langgenius.github.io/dify-enterprise-docker-compose/
- Docker
- Docker Compose
- Linux Server
  - **Warning**: Don't support Docker Desktop for Windows and Mac.
  - Minimum: 4 CPU cores, 16 GB memory, 100 GB disk space.

## Quick Start

### Download

Download a specific version from Dify Enterprise [repository](https://langgenius.github.io/dify-enterprise-docker-compose/).

Using wget:

```bash
wget https://langgenius.github.io/dify-enterprise-docker-compose/dify-docker-compose-<version>.tgz
```

Using curl:

```bash
curl -O https://langgenius.github.io/dify-enterprise-docker-compose/dify-docker-compose-<version>.tgz
```

## Installation

1. Extract the contents of the downloaded file to your deployment directory.

    ```bash
    mkdir dify-enterprise
    tar -xzf dify-docker-compose-<version>.tgz -C ./dify-enterprise/
    ```

2. Copy the environment template, then start docker compose.

    ```bash
    cd dify-enterprise
    cp .env.example .env
    docker compose up -d
    ```

3. How to Access Dify Enterprise.
    - Get the IP address of your server and change the hosts file on your local machine to point the domain to the IP address.
    ```bash
    4.152.1.216 console.dify.local
    4.152.1.216 app.dify.local
    4.152.1.216 api.dify.local
    4.152.1.216 upload.dify.local
    4.152.1.216 enterprise.dify.local
    ```

4. [Verify Dify Services Status.](/en/3.14.x/deploy/verify-services-status)

5. [License Activation.](/en/3.14.x/deploy/license-activation)

## Upgrade From Community Edition

<Note>
**Environment configuration was restructured in 3.10**: the monolithic `.env.example` is now split into a slim `.env.example` plus optional `envs/*.env.example` files, with enterprise settings shipped active under `envs/enterprise/`. Restoring your backed-up `.env` is safe — Docker Compose reads `.env` last, so your existing values take precedence and the deployment keeps working without migration. Optionally, to pick up new 3.10 defaults or newly added variables, reconcile `.env` against the new `.env.example` with the bundled `dify-env-sync.sh` (one-way `.env.example` → `.env`; preserves your custom values, backs up first, reports removed keys).
</Note>

1. Create the enterprise database (make sure your community version is running).

    ```bash
    docker exec -it dify-db-1 psql -U postgres -c "CREATE DATABASE enterprise;"
    ```

    **Tip:** You may need to adjust the container name based on your running containers. Use `docker container ls` to find the name of the `db` service.

2. Shut down the community version.

   ```bash
   docker compose down
   ```

3. Backup your data and settings (you may need to use `sudo`).

    ```bash
    tar -czf backup.tgz volumes .env # add other customized files and folders if any.
    ```

4. Delete all files and folders from the Community version, except for the `backup.tgz` and `dify-docker-compose-<version>.tgz`.
5. Extract the contents of the downloaded file to your deployment directory.

    ```bash
    tar -xzf dify-docker-compose-<version>.tgz -C /path/to/directory
    ```

6. Restore your backup by deleting the `volumes` folder.

    ```bash
    rm -rf volumes
    ```

    Then run:

    ```bash
    tar -xzf backup.tgz
    ```

7. If you have custom configurations (e.g., `docker-compose.yaml`, `ssrf_proxy/squid.conf`, or `nginx/conf.d/default.conf`), update the corresponding files with your changes.
8. Start docker compose.

    ```bash
    docker compose up -d
    ```

9. How to Access Dify Enterprise.
    - Get the IP address of your server and change the hosts file on your local machine to point the domain to the IP address.
    ```bash
    4.152.1.216 console.dify.local
    4.152.1.216 app.dify.local
    4.152.1.216 api.dify.local
    4.152.1.216 upload.dify.local
    4.152.1.216 enterprise.dify.local
    ```

10. [Verify Dify Services Status.](/en/3.14.x/deploy/verify-services-status)

11. [License Activation.](/en/3.14.x/deploy/license-activation)

## Upgrade From Previous Enterprise Edition

<Note>
**Environment configuration was restructured in 3.10**: the monolithic `.env.example` is now split into a slim `.env.example` plus optional `envs/*.env.example` files, with enterprise settings shipped active under `envs/enterprise/`. Restoring your backed-up `.env` is safe — Docker Compose reads `.env` last, so your existing values take precedence and the deployment keeps working without migration. Optionally, to pick up new 3.10 defaults or newly added variables, reconcile `.env` against the new `.env.example` with the bundled `dify-env-sync.sh` (one-way `.env.example` → `.env`; preserves your custom values, backs up first, reports removed keys).
</Note>

1. Backup your data and settings (you may need to use sudo).

    ```bash
    tar -czf backup.tgz volumes .env # add other customized files and folders if any
    ```

2. Delete all files and folders from the Community version, except for the `backup.tgz` and `dify-docker-compose-<version>.tgz`.
3. Extract the contents of the downloaded file to your deployment directory.

    ```bash
    tar -xzf dify-docker-compose-<version>.tgz -C /path/to/directory
    ```

4. Restore your backup by deleting the `volumes` folder.

   ```bash
   rm -rf volumes
   ```

   Then run:

   ```bash
   tar -xzf backup.tgz
   ```

5. If you have custom configurations (e.g., `docker-compose.yaml`, `ssrf_proxy/squid.conf`, or `nginx/conf.d/default.conf`), update the corresponding files with your changes.
6. Start docker compose:

    ```bash
    docker compose up -d
    ```

## Advanced Configuration

### Configuration Files

Configuration is split across a few files in your deployment directory:

- **`.env`** — your active configuration. Copy it from `.env.example` (`cp .env.example .env`) before the first start. Put all your overrides here; Docker Compose reads it **last**, so values in `.env` take precedence over everything else.
- **`envs/enterprise/*.env`** — enterprise service settings. Shipped **active** and wired automatically; you do not need to copy or edit them for a standard deployment.
- **`envs/*.env.example`** — optional advanced settings grouped by theme (databases, vectorstores, infrastructure, …). To enable a group, copy the file without the `.example` suffix (for example `cp envs/databases/redis.env.example envs/databases/redis.env`).

For the settings below, adding the variables to `.env` is sufficient — they override any default from the files above.

### Domain Requirements

Dify Enterprise requires 5 domains to be set up. The domains are:

- Console: console.dify.local
- Services API: api.dify.local
- APP: app.dify.local
- Files: upload.dify.local
- Enterprise: enterprise.dify.local

To change this, add the relevant configurations to the `.env` file in your deployment directory.

```bash
# .env

CONSOLE_API_URL="http://console.dify.local"
CONSOLE_WEB_URL="http://console.dify.local"
SERVICE_API_URL="http://api.dify.local"
APP_API_URL="http://app.dify.local"
APP_WEB_URL="http://app.dify.local"
FILES_URL="http://upload.dify.local"
ENTERPRISE_URL="http://enterprise.dify.local"
```

​

### Persistence Storage

By defualt, Enterprise Edition uses **local storage**. To change this, add the relevant configurations in the `.env` file in your deployment directory. For example, to use **AWS S3**:

```bash
# .env

STORAGE_TYPE=s3

S3_USE_AWS_MANAGED_IAM=
S3_ENDPOINT=
S3_REGION=
S3_BUCKET_NAME=
S3_ACCESS_KEY=
S3_SECRET_KEY=
```

For configurations for other storage options, refer to the [File Storage Configuration](https://docs.dify.ai/en/self-host/deploy/configuration/environments#file-storage-configuration).

### Postgres Database

To change this, add the relevant configurations to the `.env` file in your deployment directory.

```bash
# .env

DB_USERNAME=postgres
DB_PASSWORD=difyai123456
DB_HOST=db
DB_PORT=5432
DB_DATABASE=dify
```

For configurations for other databases, refer to the [Database Configuration](https://docs.dify.ai/en/self-host/deploy/configuration/environments#database-configuration).

### Redis Database

To change this, add the relevant configurations to the `.env` file in your deployment directory.

```bash
# .env

REDIS_HOST=redis
REDIS_PORT=6379
REDIS_USERNAME=
REDIS_PASSWORD=difyai123456
REDIS_USE_SSL=false
REDIS_DB=0
```

For configurations for other databases, refer to the [Redis Configuration](https://docs.dify.ai/en/self-host/deploy/configuration/environments#redis-configuration).

### Vector Database

By defualt, Enterprise Edition uses **Weaviate** as vector database. To change this, add the relevant configurations to the `.env` file in your deployment directory. For example, to use **Qdrant**:

```bash
# .env

VECTOR_STORE=qdrant

QDRANT_URL=http://qdrant:6333
QDRANT_API_KEY=difyai123456
QDRANT_CLIENT_TIMEOUT=20
QDRANT_GRPC_ENABLED=false
QDRANT_GRPC_PORT=6334
```

For configurations for other vector databases, refer to the [Vector Database Configuration](https://docs.dify.ai/en/self-host/deploy/configuration/environments#vector-database-configuration).

### Agent Sandbox Backend

The Agent's sandbox runs on one of two backends, selected with `DIFY_AGENT_RUNTIME_BACKEND` in the `.env` file. Pick exactly one backend and set its variable family; the choice also determines where the agent's Home Snapshot (persistent files) lives.

By default the deployment uses the bundled `local_sandbox` container:

```bash
# .env

DIFY_AGENT_RUNTIME_BACKEND=local
DIFY_AGENT_LOCAL_SANDBOX_ENDPOINT=http://local_sandbox:5004
DIFY_AGENT_LOCAL_SANDBOX_AUTH_TOKEN=
```

The legacy `DIFY_AGENT_SHELLCTL_ENTRYPOINT` and `DIFY_AGENT_SHELLCTL_AUTH_TOKEN` names remain accepted as fallbacks for existing deployments.

To run sandboxes on [E2B](https://e2b.dev/) instead, start the stack with the `docker-compose.e2b.yaml` overlay and set the E2B family:

```bash
# .env

DIFY_AGENT_E2B_API_KEY=your-e2b-api-key
DIFY_AGENT_E2B_TEMPLATE=difys-default-team/dify-agent-local-sandbox
DIFY_AGENT_E2B_ACTIVE_TIMEOUT_SECONDS=3600
DIFY_AGENT_E2B_SHELLCTL_PORT=5004
```

- `DIFY_AGENT_E2B_API_KEY`: your E2B API key. `E2B_API_KEY` and `E2B_API_TOKEN` remain accepted as deployment-level fallbacks.
- `DIFY_AGENT_E2B_TEMPLATE`: the E2B sandbox template, default `difys-default-team/dify-agent-local-sandbox`.
- `DIFY_AGENT_E2B_ACTIVE_TIMEOUT_SECONDS`: maximum continuous active time for one sandbox lease, default `3600`. Its default matches the independently configurable agent run deadline (`DIFY_AGENT_RUN_TIMEOUT_SECONDS`).
- `DIFY_AGENT_E2B_SHELLCTL_PORT`: port the sandbox runtime listens on, default `5004`.

### Additional Configuration

For additional configuration options, refer to the [Environment Variables Explanation](https://docs.dify.ai/en/self-host/deploy/configuration/environments).
