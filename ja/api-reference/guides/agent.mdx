---
title: New Agent API
sidebarTitle: New Agent（Beta）
description: ストリーミングチャットメッセージ、会話管理、ファイルアップロード、アプリ設定を扱う New Agent アプリ向けの API
tag: Beta
---

> このドキュメントは AI によって自動翻訳されています。不正確な部分がある場合は、[英語版](/en/api-reference/guides/agent) を参照してください。

New Agent アプリは `agent` モードで動作します。他のチャット形式アプリと同じく、メッセージを送信し、回答をストリーミングで受け取ります。会話も共通のエンドポイントで管理します。

モデルは自律的に推論し、ツールを使用しますが、その過程はストリームには公開されません。ストリームに含まれるのは、通常の `message` イベントのみです。`message_end` イベントの `metadata` には `usage` が含まれます。アノテーションに一致した場合は `annotation_reply` も含まれます。`retriever_resources` が含まれることはありません。

<Note>
New Agent アプリタイプは現在ベータ版です。
</Note>

## メッセージの送信とストリーミング返信

- **[チャットメッセージを送信](/ja/api-reference/chat-messages/send-chat-message)**：New Agent アプリに質問を送信します。ストリーミングモードのみに対応し、ブロッキングモードを指定すると 400 `bad_request` が返されます。
- **[生成を停止](/ja/api-reference/chat-messages/stop-chat-message-generation)**：ストリーミング中の返信を完了前に中断します。

## 会話の管理

- **[会話一覧を取得](/ja/api-reference/conversations/list-conversations)**：現在のユーザーの会話を、最終アクティビティの新しい順に返します。
- **[会話履歴メッセージ一覧を取得](/ja/api-reference/conversations/list-conversation-messages)**：1 つの会話のメッセージ履歴を返します。スクロール表示のチャット UI に適しています。履歴にも推論過程は含まれず、各メッセージの `agent_thoughts` 配列は常に空です。

## ファイルのアップロード

- **[ファイルをアップロード](/ja/api-reference/files/upload-file)**：画像、ドキュメント、音声、動画ファイルをアップロードします。ファイルはアップロードしたエンドユーザーのみが利用できます。New Agent アプリはファイル入力に未対応です。[チャットメッセージを送信](/ja/api-reference/chat-messages/send-chat-message) にファイル参照を渡しても、受け付けられるだけで使用されません。

## アプリ情報と設定の取得

- **[アプリケーションの基本情報を取得](/ja/api-reference/applications/get-app-info)**：アプリの名前、説明、タグ、モードを返します。
- **[アプリケーションのパラメータ情報を取得](/ja/api-reference/applications/get-app-parameters)**：入力フォームの設定（機能スイッチ、パラメータの名前・型・デフォルト値）を返します。クライアント UI の構築に役立ちます。
- **[アプリケーションのメタ情報を取得](/ja/api-reference/applications/get-app-meta)**：ツールアイコンなどの設定メタデータを返します。
