---
title: トラブルシューティング
description: difyctl のよくあるエラーを、原因の説明、終了コード、解決策とともに修正する
---

> このドキュメントは AI によって自動翻訳されています。不正確な部分がある場合は、[英語版](/en/cli/troubleshooting) を参照してください。

`difyctl` はエラーが発生すると、メッセージを stderr に書き込みます。`-o json` を有効にすると、エラーも単一行の JSON として出力され、安定した `code` フィールドと対処可能な `hint` が含まれます。このページでは、遭遇する可能性が高いエラーを、それを引き起こした操作ごとにまとめて説明します。

終了コードの完全な一覧表とエラーオブジェクトの構造については、[出力形式と終了コード](/ja/cli/reference/output-formats-and-exit-codes) を参照してください。

## 認証エラー

### `auth_expired`

セッションが期限切れになり、`difyctl` は認証済みリクエストを実行できなくなりました。

1. 再認証します。

    ```bash
    difyctl auth login
    ```

2. 失敗したコマンドを再実行します。

終了コード：`4`。

### `not_logged_in`

要求されたホストに対する有効なセッションがありません。

1. サインインします。

    ```bash
    difyctl auth login --host <url>
    ```

2. 失敗したコマンドを再実行します。

終了コード：`4`。

### `access_denied`

サインインリクエストを拒否したか、承認前にキャンセルしたため、何も認可されていません。

再実行してリクエストを承認します。

```bash
difyctl auth login
```

終了コード：`4`。

## アプリエラー

### アプリが見つからない（HTTP 404）

指定したアプリ ID が存在しないか、お使いの token ではアクセスできません。`server_4xx_other` として、HTTP ステータス 404 で表示されます。

1. アクセスできるアプリを一覧表示します。

    ```bash
    difyctl get app
    ```

2. ID を確認して再実行します。

終了コード：`1`。

### サービス API が無効（サーバーから HTTP 403）

アプリは存在しますが、そのサービス API が Dify コンソールでオフになっています。アプリのオーナーが有効にする必要があります。403 は `server_4xx_other` として表示されます。

終了コード：`1`。

## ワークフローエラー

### ワークフローの検証エラー（HTTP 422）

ワークフローアプリが入力を拒否しました。エラーメッセージには、不足している、または無効な必須入力が一覧表示されます。

1. アプリの入力 schema を確認します。

    ```bash
    difyctl describe app <id>
    ```

2. `--inputs` の JSON を一致するように更新します。

終了コード：`1`。
